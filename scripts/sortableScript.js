Sortable.create(A, {
    group: "shared",
    sort: true,
  });
  
Sortable.create(B, {
    group: "shared",
    sort: true,
  });

Sortable.create(C, {
    group: "shared",
    sort: true,
  });

  Sortable.create(D, {
    group: "shared",
    sort: true,
  });